{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "scripts": {
    "postinstall": "apt-get update && apt-get install -y ffmpeg"
  }
}
